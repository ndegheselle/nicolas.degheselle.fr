---
  import Jobs from "./Jobs.svelte";
  import jobs from "../../content/jobs.json";
  // Display date as text with ramping up animation
  // add total on the side in background
  // option to hide interneship
---

<Jobs jobs={jobs}/>